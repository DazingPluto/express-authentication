<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/animePage.css">
    <title>Animes</title>
</head>
<body>
    <div class='headerBar'>
    <h1 class='commentSection'>Anime</h1>
    </div>
    <% for( let index = 0; index < animeTop.length; index++ ) { %>
    <div>
        <% let a = animeTop[index]; %> 
        <p><%= a.title %></p>
        <a href="http://localhost:3000/anime/<%=a.mal_id%>">
            <img src="<%= a.image_url %>" alt="">
        </a>
        <hr>
    <% } %>
    </div>
</body>
</html>